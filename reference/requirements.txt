# Standalone Coloring Book Drawer App
# Complete requirements including shared utilities
# ================================================

# Core GUI & Graphics
pygame>=2.5.0
PySide6>=6.6.0
PySide6-Addons>=6.6.0

# Image Processing & Computer Vision
numpy>=1.24.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0
scipy>=1.11.0

# GPU acceleration (optional - for CUDA 12.x NVIDIA GPUs):
# Uncomment the line below if you have an NVIDIA GPU with CUDA support
# cupy-cuda12x

# Note: FFmpeg is required for video generation but must be installed separately
# Windows: Download from https://ffmpeg.org/download.html
# Linux: sudo apt-get install ffmpeg
# macOS: brew install ffmpeg
